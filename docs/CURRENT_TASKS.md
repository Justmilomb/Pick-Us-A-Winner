# Current Tasks — Giveaway Engine

## Completed

- Full-stack Instagram giveaway runner (React + Express + PostgreSQL)
- Instagram comment scraping (Puppeteer + API fallback)
- Winner selection with filtering rules
- Stripe payment integration
- User authentication (Passport.js local strategy)
- Email notifications (Nodemailer)
- Background job scheduler for pending giveaways
- Raspberry Pi scraper relay setup
- Docker containerisation
- Production build pipeline (Vite + esbuild)

## In Progress

(nothing currently)

## Up Next

- Set up automated test framework (no tests exist)
- Clean up runtime artifacts from repo root (db.json, worker-jobs.json, scraper-debug.log)
- Audit credentials.json — should not be in repo root
- Create docs/systems/ deep-dive files for key subsystems (scraper, payment, scheduler)
